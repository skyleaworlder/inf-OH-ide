#include <cmath>
#include <limits>
#include <iostream>

double gammln(const double input);
std::pair<double, double> gser(const double alpha, const double x);
double gammp(const double a, const double x);
double gammq(const double a, const double x);